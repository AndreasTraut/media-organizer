# --- System & Helper ---
python-dotenv      # WICHTIG: Für load_dotenv() in Zeile 30/25
numpy
scipy
scikit-learn       # Für fetch_demo_pictures.py (LFW Datensatz)

# --- Bildverarbeitung ---
Pillow             # Nötig für Image.open() in allen Skripten
opencv-python      # Wichtiges Backend für DeepFace/FER

# --- Für photo_insights.py (Gesichter & Emotionen) ---
deepface           # Ihre Wahl für Gesichtserkennung (besser als dlib auf Windows)
tf-keras           # Wird von DeepFace benötigt
fer==22.5.1        # Für Emotionserkennung

# --- Für photo_rag.py (Suche & Vektoren) ---
faiss-cpu          # Ihre Vektor-Datenbank (direkter Import im Code)
chromadb           # Optionaler Import in Ihrem Code
openai             # Für die Chat-Funktion

# --- KI-Modelle (CLIP) ---
torch              # Backend für Transformers & CLIP
torchvision        # Bild-Funktionen für Torch
transformers       # Lädt das CLIP Modell in photo_rag.py
ftfy               # Hilfspaket für Text-Encoding in CLIP

# Original CLIP (Fallback in Ihrem Code)
git+https://github.com/openai/CLIP.git